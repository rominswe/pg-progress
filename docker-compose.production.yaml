version: "3.9"

services:
  server:
    build: ./server
    container_name: pg-progress-server-prod
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env.production
    # For production, use external database and Redis services
    # depends_on:
    #   - mysql
    #   - redis
    networks:
      - app-network
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  # Production database and Redis should be external/managed services
  # Uncomment and configure if using containerized production services

  # mysql:
  #   image: mysql:8.0
  #   container_name: pg-progress-mysql-prod
  #   restart: always
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE}
  #     MYSQL_USER: ${MYSQL_USER}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   volumes:
  #     - mysql_prod_data:/var/lib/mysql
  #   networks:
  #     - app-network

  # redis:
  #   image: redis:8.0-alpine
  #   container_name: pg-progress-redis-prod
  #   restart: always
  #   networks:
  #     - app-network

# volumes:
#   mysql_prod_data:

networks:
  app-network:
    driver: bridge